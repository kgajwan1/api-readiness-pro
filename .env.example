# Configure any one (or more) of these to enable AI-assisted remediation
# via /api/analyze. None are required — the deterministic engine works
# with zero keys configured. Keys can also be pasted into the UI's
# Policy Settings tab at runtime instead of being set here.
GEMINI_API_KEY="MY_GEMINI_API_KEY"
OPENAI_API_KEY="MY_OPENAI_API_KEY"
ANTHROPIC_API_KEY="MY_ANTHROPIC_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"
